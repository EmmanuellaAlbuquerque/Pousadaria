

<%= form_with(model: @sale) do |f| %>
  <div class="mb-3">
    <%= f.label :name, 'Nome' %>
    <%= f.text_field :name %>
  </div>

  <div class="mb-3">
    <%= f.label :start_date, 'Data de início' %>
    <%= f.date_field :start_date %>
  </div>

  <div class="mb-3">
    <%= f.label :end_date, 'Data de fim' %>
    <%= f.date_field :end_date %>
  </div> 

  <div class="mb-3">
    <%= f.label :inn_room, 'Quarto' %>
    <%= f.collection_select :inn_room_id, @inn_rooms, :id, :name %>
  </div>

  <div>
    <%= f.label :discount_percentage, 'Valor do desconto (em %)' %>
    <%= f.number_field :discount_percentage %>
  </div>

  <%= f.submit class: 'btn btn-success'  %>
<% end %>